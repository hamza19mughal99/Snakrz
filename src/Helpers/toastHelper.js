
export function deleted(toast) {
	return toast('Deleted Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function created(toast) {
	return toast('Created Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}